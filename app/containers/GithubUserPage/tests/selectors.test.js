// import { fromJS } from 'immutable';
// import { makeSelectGithubUserContainerDomain } from '../selectors';

// const selector = makeSelectGithubUserContainerDomain();

describe('makeSelectGithubUserContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
