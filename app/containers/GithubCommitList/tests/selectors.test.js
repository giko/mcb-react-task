// import { fromJS } from 'immutable';
// import { makeSelectGithubCommitListDomain } from '../selectors';

// const selector = makeSelectGithubCommitListDomain();

describe('makeSelectGithubCommitListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
