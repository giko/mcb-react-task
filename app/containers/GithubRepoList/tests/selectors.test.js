// import { fromJS } from 'immutable';
// import { makeSelectGithubRepoListDomain } from '../selectors';

// const selector = makeSelectGithubRepoListDomain();

describe('makeSelectGithubRepoListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
